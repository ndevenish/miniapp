#pragma once

__global__ void erosion_kernel(uint8_t *mask,
                               size_t mask_pitch,
                               int width,
                               int height,
                               int radius);